name = "mortipins-dashboard"
main = "../dashboard_update_points.js"
compatibility_date = "2026-02-02"

[[d1_databases]]
binding = "DB"
database_name = "mortipins"
database_id = "9f64aa01-a10f-4409-bccb-a38a97946f4d"

# Secrets for token verification (if you use JWT)
# Use: wrangler secret put JWT_SECRET
# env vars can be added under [env.<name>] if needed